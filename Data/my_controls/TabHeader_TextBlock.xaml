<UserControl
    x:Class="Fakturka.TabHeader_TextBlock"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fakturka"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" x:Name="TextBlock1" FontSize="27" Margin="15,0,0,0" Foreground="SteelBlue" FontFamily="{Binding Font}" Text="{Binding Icon}"/>

        <TextBlock Grid.Column="1" x:Name="TextBlock2" FontSize="21" Margin="8,0,0,0" Text="{Binding Text}"/>
    </Grid>
</UserControl>
